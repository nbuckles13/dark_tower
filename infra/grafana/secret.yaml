apiVersion: v1
kind: Secret
metadata:
  name: grafana-secrets
  namespace: dark-tower-observability
type: Opaque
stringData:
  # Development password - override in production via external-secrets or sealed-secrets
  GF_SECURITY_ADMIN_PASSWORD: "admin"
