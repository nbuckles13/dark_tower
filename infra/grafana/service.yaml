apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: dark-tower-observability
spec:
  selector:
    app: grafana
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30030
  type: NodePort
