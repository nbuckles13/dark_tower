apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - secret.yaml
  - pvc.yaml
  - statefulset.yaml
  - service.yaml
  - network-policy.yaml

labels:
  - pairs:
      managed-by: dark-tower
    includeSelectors: false
