apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - configmap.yaml
  - statefulset.yaml
  - service.yaml
  - secret.yaml
  - network-policy.yaml

labels:
  - pairs:
      managed-by: dark-tower
    includeSelectors: false
