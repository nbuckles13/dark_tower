apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: dark-tower
  labels:
    app: redis
    component: cache
spec:
  type: ClusterIP
  selector:
    app: redis
    component: cache
  ports:
  - name: redis
    protocol: TCP
    port: 6379
    targetPort: 6379
  sessionAffinity: None
