apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: mh-service
  namespace: dark-tower
  labels:
    app: mh-service
    component: media
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: mh-service
      component: media
