apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../../../services/gc-service
  - nodeport.yaml

labels:
  - pairs:
      environment: kind
    includeSelectors: false
