# Devloop Output: wtransport 0.1 → 0.7 bump

**Date**: 2026-05-11
**Task**: Bump workspace `wtransport` dep from "0.1" to "0.7" (closes 5 cargo-audit wtransport-stack advisories + 3 unmaintained warnings; leaves only RUSTSEC-2023-0071 rsa-via-sqlx visible).
**Specialist**: infrastructure
**Mode**: Agent Teams (light, 3 teammates: implementer + security + test)
**Branch**: `feature/browser-client-join-task33`
**Duration**: TBD

---

## Loop Metadata

| Field | Value |
|-------|-------|
| Start Commit | `b8752d692d7a8da20ee2e6b8ca9051a54a0b0711` |
| Branch | `feature/browser-client-join-task33` |
| Fast-Fail Probe | 2026-05-10 (in-session, no commit) — established 2-line breakage + 9→1 advisory closure |

---

## Loop State (Internal)

| Field | Value |
|-------|-------|
| Phase | `complete` |
| Implementer | `implementer@devloop-2026-05-11-wtransport-0-7-bump` |
| Implementing Specialist | `infrastructure` |
| Iteration | `1` |
| Security | `security@devloop-2026-05-11-wtransport-0-7-bump` |
| Test (context reviewer) | `test@devloop-2026-05-11-wtransport-0-7-bump` |

### --light Eligibility Override

The change touches workspace `Cargo.toml`, which is on the SKILL.md `--light`
ineligibility list. User-Lead explicitly invoked `--light` after seeing the
fast-fail-probe results (2026-05-10): the actual diff is 2 source lines +
the Cargo.toml bump, with `cargo check --workspace --all-targets` clean
after the 2-line fix. User-Lead's call is on the record; SKILL eligibility
check overridden.

---

## Task Overview

### Objective

Land the wtransport 0.1 → 0.7 bump. Two source edits + one workspace dep
declaration:

1. **Workspace `Cargo.toml`** — `wtransport = "0.1"` → `wtransport = "0.7"`
   (line 57).
2. **`crates/mc-service/src/webtransport/server.rs:129`** — change
   `.with_identity(&identity)` to `.with_identity(identity)`.
3. **`crates/mh-service/src/webtransport/server.rs:125`** — same change.

That is the entire diff. Probe-confirmed.

### Why this matters

Closes the predecessor devloop's accepted Layer 6 cargo-audit deferral:
- RUSTSEC-2026-0037 quinn-proto (Denial of service in Quinn endpoints)
- RUSTSEC-2025-0009 ring 0.16.20 (AES panic on overflow check)
- RUSTSEC-2026-0098 rustls-webpki 0.101.7 (URI name constraints)
- RUSTSEC-2026-0099 rustls-webpki 0.101.7 (wildcard name constraints)
- RUSTSEC-2026-0104 rustls-webpki 0.101.7 (CRL panic)
- RUSTSEC-2025-0010 ring 0.16.20 (unmaintained warning)
- RUSTSEC-2025-0134 rustls-pemfile 1.0.4 (unmaintained warning)
- RUSTSEC-2025-0134 rustls-pemfile 2.2.0 (unmaintained warning)

After the bump, only RUSTSEC-2023-0071 (rsa via sqlx-mysql) remains — as
the user-Lead's no-allowlist principle expects (it stays visible until
upstream or codebase changes provide a closure path).

### Scope

- **Service(s)**: mc-service + mh-service (both server.rs edits in
  `src/webtransport/`).
- **Schema**: No.
- **Cross-cutting**: workspace `Cargo.toml` dep version, but no behavioral
  change to any service-level code beyond the by-value-vs-by-reference
  Identity passing convention.

### Debate Decision

NOT NEEDED — fast-fail probe definitively established scope. Original
"5 major versions of API change" framing in the predecessor's spin-out
plan was a paper-analysis overestimate; the 30-second probe (bump
Cargo.toml, run `cargo check --workspace --all-targets`) showed the
actual breakage is one signature change to `ServerConfig::with_identity()`.
Lessons-Learned will record this so the next supply-chain hygiene devloop
prefers empirical probing over paper inventory.

---

## Cross-Boundary Classification

| Path | Classification | Owner (if not mine) | Note |
|------|----------------|---------------------|------|
| `Cargo.toml` | Mine | — | workspace `wtransport` version bump |
| `crates/mc-service/src/webtransport/server.rs` | Mine | — | `with_identity` arg by-value adjustment |
| `crates/mh-service/src/webtransport/server.rs` | Mine | — | `with_identity` arg by-value adjustment |
| `docs/devloop-outputs/2026-05-11-wtransport-0-7-bump/main.md` | Mine | — | devloop output |

`Cargo.lock` is auto-regenerated by the dep bump but is gitignored in this
workspace, so it does not appear in the commit diff and is intentionally
omitted from the table above (it would trip the scope-drift guard as a
planned-but-untouched entry). The gitignored-Cargo.lock convention is
itself a tracked tech-debt item — see Tech Debt References below.

**Classification rationale**:

- All paths are infrastructure-domain (workspace dep declaration + the
  two by-value Identity fixes that flow directly from the dep bump).
- mc-service and mh-service `src/webtransport/server.rs` edits are **NOT**
  meeting-controller or media-handler domain in the substantive sense
  — they're mechanical signature adjustments forced by the upstream API
  change, not behavioral changes to MC/MH semantics. By the §6.2 sed-test
  they pass: deterministic find-and-replace, structure-preserving, fully
  caught by `cargo check`. **Mechanical-leaning Mine.**
- No paths fall inside ADR-0024 §6.4 Guarded Shared Areas:
  - `crates/common/src/webtransport/**` is GSA, but this devloop doesn't
    touch any file there (verified: probe diff was 2 files, both under
    `crates/{mc,mh}-service/src/webtransport/`).
  - `proto/**` not touched.
  - No auth/crypto primitives touched (the `Identity` type is loaded from
    PEMs via `Identity::load_pemfiles`, no construction-side cert work).

---

## Pre-Work

None. Predecessor commit `b8752d6` is the start commit. Fast-fail probe at
2026-05-10 in-session established the diff shape without committing.

---

## Implementation Plan (light mode — no Gate 1)

Steps 1-4 are mechanical:

1. Edit `/work/Cargo.toml` line 57: `wtransport = "0.1"` → `wtransport = "0.7"`.
2. Edit `/work/crates/mc-service/src/webtransport/server.rs:129`:
   `.with_identity(&identity)` → `.with_identity(identity)`.
3. Edit `/work/crates/mh-service/src/webtransport/server.rs:125`: same.
4. Sanity: `cargo check --workspace --all-targets` clean. `cargo audit`
   reports exactly 1 advisory (RUSTSEC-2023-0071 rsa). Verify no other
   per-crate `Cargo.toml` overrides exist that pin wtransport (we already
   confirmed: mc-service/mh-service/env-tests all use `workspace = true`).
5. SendMessage @team-lead "Ready for validation".

If Layer 4 (`cargo test --workspace`) or Layer 8 (env-tests) surface any
test regression that the probe's `cargo check` didn't catch, narrow the
fix to the specific call site rather than rolling back the bump. The 2-line
fix is non-negotiable; any third-edit-required-by-test-failure becomes a
new finding to address.

---

## Implementation Summary

Applied the probe-confirmed 3-edit diff exactly as specified:

1. `/work/Cargo.toml:57` — `wtransport = "0.1"` → `wtransport = "0.7"`.
2. `/work/crates/mc-service/src/webtransport/server.rs:129` — `.with_identity(&identity)` → `.with_identity(identity)`.
3. `/work/crates/mh-service/src/webtransport/server.rs:125` — `.with_identity(&identity)` → `.with_identity(identity)`.

No additional source edits required. `cargo check --workspace --all-targets` is clean. `cargo audit` reports exactly 1 remaining advisory (RUSTSEC-2023-0071 rsa-via-sqlx-mysql, out-of-scope per the user-Lead no-allowlist principle). `git diff --stat` shows exactly 3 source files touched (`Cargo.lock` regenerated but gitignored, as expected).

---

## Files Modified

```
 Cargo.toml                                   | 2 +-
 crates/mc-service/src/webtransport/server.rs | 2 +-
 crates/mh-service/src/webtransport/server.rs | 2 +-
 3 files changed, 3 insertions(+), 3 deletions(-)
```

### Key Changes by File

| File | Changes |
|------|---------|
| `Cargo.toml` | Workspace `wtransport` dep: `"0.1"` → `"0.7"` (closes 5 cargo-audit advisories + 3 unmaintained warnings via lockfile cascade) |
| `crates/mc-service/src/webtransport/server.rs` | Line 129: `.with_identity(&identity)` → `.with_identity(identity)` |
| `crates/mh-service/src/webtransport/server.rs` | Line 125: `.with_identity(&identity)` → `.with_identity(identity)` |

`Cargo.lock` is gitignored (predecessor's Cluster B loose-end stands —
Cargo.lock tracking workflow change deferred to a separate devloop).

---

## Devloop Verification Steps

TBD — populated at Gate 2.

### Layer 1: cargo check (compile)
**Status**: PASS. `cargo check --workspace --all-targets` finished clean — `Checking mc-service`, `Checking mh-service`, `Checking mc-test-utils`, then `Finished `dev` profile [unoptimized + debuginfo] target(s) in 1.44s`. No warnings or errors surfaced by the wtransport 0.7 bump beyond the 2 by-value `with_identity` adjustments already applied.

### Layer 2: cargo fmt
**Status**: TBD

### Layer 3: Guards (always-run) + predicate-meta-test
**Status**: TBD

### Layer 4: Unit tests
**Status**: 372/373 pass. The single failure is `test_issue_user_token_timing_attack_prevention` in `crates/ac-service/src/services/token_service.rs` — a wall-clock timing-variance assertion (`MAX_TIMING_VARIANCE_PERCENT = 30.0%`) between existing-vs-nonexistent user paths in `issue_user_token`. **Pre-existing flake, deferred**: team-lead reproduced the same failure on the start commit `b8752d6` after stashing the 3 source edits, so this is NOT caused by the wtransport bump. `ac-service` has no wtransport / quinn-proto / ring usage in the `issue_user_token` path. Deferral is valid per `review-protocol.md` §Fix-or-Defer ("requires changing files outside the PR's changeset" — fix lives in ac-service test code, not in this devloop's scope, and it's flake stabilization, not wtransport-bump concern). Tracked in Tech Debt References below.

### Layer 5: clippy
**Status**: TBD

### Layer 6: Audit (always-run) — pnpm audit + cargo audit
**Status**: Probe-predicted result confirmed. `cargo audit` (advisory-db loaded, 1068 advisories, 468 crate deps scanned) reports exactly 1 vulnerability: **RUSTSEC-2023-0071 rsa 0.9.10** ("Marvin Attack: potential key recovery through timing sidechannels", severity 5.9 medium, no fixed upgrade available), reaching the workspace via `sqlx-mysql 0.8.6` → `sqlx 0.8.6` → `{ac,gc}-{service,test-utils}`. All 5 wtransport-stack advisories (quinn-proto / ring / rustls-webpki ×3) plus the 3 unmaintained warnings (ring 0.16.20 + rustls-pemfile 1.0.4 + rustls-pemfile 2.2.0) closed by the lockfile cascade. Layer 6 remains FAIL purely because the rsa advisory remains visible per the user-Lead no-allowlist principle — out of scope for this devloop.

### Layer 7: Semantic guards
**Status**: TBD

### Layer 8: Env-tests
**Status**: TBD — **load-bearing**: integration tests against live Kind cluster will exercise the new wtransport 0.7 wire behavior end-to-end (mc-service ↔ mh-service via WebTransport).

---

## Code Review Results

### Security Specialist
**Verdict**: **CLEAR** — 0 findings. 7/7 checkpoints pass: diff scope (3 source files), no allowlist landed, `with_identity(identity)` ownership semantics clean (binding consumed exactly once, no later borrow), `Identity::load_pemfiles` flow unchanged, no GSA paths in diff, Layer 6 advisory delta = 1 (rsa) as designed, rsa stays visible. Ownership Lens: mc/mh server.rs edits confirmed Mechanical-leaning Mine; no upgrade challenge. Deferred items accepted (timing-flake on start commit, Layers 1/2/5 SKIPPED-NO-VERB dispatcher artifact, Layer 7 wave2-pending).

### Test Specialist (context reviewer)
**Verdict**: **CLEAR** — 0 findings. Test rig compatibility confirmed: 6 wtransport-using test files compile under 0.7 unchanged; `dangerous-configuration` feature stable in all 3 test crates; no `#[ignore]` additions in diff. Layer 4 (372/373) accepted with timing-flake disposition. Layer 8 (env-tests) not required for this verdict — component-tier integration tests already exercise the production `Identity::load_pemfiles` + `accept_loop` + `wt_client` path with real wtransport 0.7 wire frames; end-to-end coverage at this scope would only add k8s-orchestration which the bump doesn't touch.

---

## Tech Debt References

TBD — appended at completion.

Pre-existing tech debt items unaffected by this devloop:
- **RUSTSEC-2023-0071 rsa** stays visible per user-Lead's no-allowlist
  principle. Closure path requires upstream sqlx restructure OR
  workspace switch off sqlx. Not in scope.
- **`Cargo.lock` gitignored** (predecessor's loose end). Workspace
  binary-project convention is to commit Cargo.lock; deferred to a
  separate devloop with explicit user-Lead opt-in to the workflow change.
- **`test_issue_user_token_timing_attack_prevention` flake** in
  `crates/ac-service/src/services/token_service.rs`. Wall-clock
  timing-variance assertion exceeds the 30% threshold on this runner
  environment. Reproduced on start commit `b8752d6` without the
  wtransport diff applied → confirmed pre-existing, not caused by this
  devloop. Needs a dedicated flake-stabilization devloop in the
  ac-service domain (likely: switch variance assertion to a percentile-
  or rank-based stability check, or quarantine under
  `#[ignore = "flaky-timing"]` until stabilized).

---

## Rollback Procedure

If this devloop needs to be reverted:
1. Verify start commit: `b8752d692d7a8da20ee2e6b8ca9051a54a0b0711`.
2. Review changes: `git diff b8752d6..HEAD`.
3. Hard reset: `git reset --hard b8752d6`.
4. No schema/migration changes — `git reset` is sufficient.
5. `Cargo.lock` regenerates from `Cargo.toml` on next cargo invocation.

---

## Issues Encountered & Resolutions

TBD.

---

## Lessons Learned

TBD — at minimum:

1. **Empirical probe > paper inventory** for dep-bump scope estimation.
   The 30-second `cargo check` probe falsified a "multi-day, /debate
   first" plan into a `--light` 2-line edit. Carrying-forward heuristic:
   for any dep bump that crosses 2+ major versions, run the probe before
   estimating scope.

---

## Appendix: Verification Commands

```bash
# Full polyglot pipeline (canonical Gate 2).
./scripts/verify-completion.sh --layer full

# wtransport bump verification (probe-equivalent).
cargo check --workspace --all-targets
cargo test --workspace
cargo audit

# Confirm advisory count.
cargo audit 2>&1 | grep -c "^Crate:"
# expected post-bump: 1 (RUSTSEC-2023-0071 rsa via sqlx-mysql)

# Verify wtransport version landed.
grep "^wtransport" /work/Cargo.toml
# expected: wtransport = "0.7"
```
