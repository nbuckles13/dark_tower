# Security Navigation

## Architecture & Design
- Service auth (OAuth 2.0 Client Credentials) → ADR-0003 | Token lifetime & refresh → ADR-0007 | Key rotation → ADR-0008 | User auth & meeting access → ADR-0020
- PII / generic-error discipline → ADR-0011 | No-panic policy → ADR-0002 | Approved algorithms → ADR-0027 | Unattended story runner (pass/fail authority, container boundary, suppression gate) → ADR-0035
- MC session binding & HKDF key hierarchy → ADR-0023 §1 | Client architecture (E2EE, key management, supply chain) → ADR-0028 §§1,5
- End-to-end media flow — frame signing §2, roster identity §3, KEK custody & sender_id §4, send directive §5, MH keyless/type-blind §7, control plane §8, forwarding graph §9, transport seam §10, telemetry deny §11 → ADR-0036
- Service-owned dashboards and alerts → ADR-0031 | Alert-rules guard (URL exfil + annotation hygiene) → `scripts/guards/simple/validate-alert-rules.sh`
- Cross-boundary ownership (GSA, `Approved-Cross-Boundary:` trailer, intersection rule) → ADR-0024 §6 | GSA mirror → `.claude/skills/devloop/SKILL.md`, `.claude/skills/devloop/review-protocol.md` | manifest + classification guard → `scripts/guards/simple/cross-boundary-ownership.yaml`, `validate-cross-boundary-classification.sh`
- Guard pipeline as one Rust binary → ADR-0034 | path-containment gate → `crates/dt-guard/src/common/path_safety.rs` | PII vocabulary (security + observability own the content) → `crates/dt-guard/src/common/pii_vocabulary.rs` | compound-name segment matcher (word-boundary regex cannot see inside camelCase/snake_case) → `crates/dt-guard/src/ts_retained_credentials.rs:segments()`
- Client credential-lifetime enforcement → `crates/dt-guard/src/ts_retained_credentials.rs`, wrapper `scripts/guards/simple/ts/no-retained-credentials.sh` | semantic lens → `scripts/guards/semantic/checks.md` §Client Credential Lifetime | token-based join → `packages/sdk-core/src/session/events.ts::TokenCredentials`, `packages/web-app/src/lib/types.ts::AuthSession`

## Media Path — Keys, Identity, Frame Trust Boundary (ADR-0036)
- Wire layout, key-id field widths, size caps (`MAX_PAYLOAD_BYTES`, `MAX_FRAME_BYTES`, `MAX_EXT_BYTES`) → `crates/media-protocol/src/frame.rs`; extension registry → `crates/media-protocol/src/extensions.rs`
- Untrusted-bytes parse surface + relay rewrite → `crates/media-protocol/src/codec.rs:decode_datagram()`, `decode_stream_frame()`, `peek_frame_len()`, `rewrite_relay_region()`
- MC is the sole issuing authority for KEK / identity key / sender_id; TOFU security floor stated once → `crates/mc-service/src/media_admission/mod.rs` | Per-meeting KEK (CSPRNG, `SecretBox`, no exposure path) + generation counter → `crates/mc-service/src/media_admission/kek.rs:MeetingKek::generate()`, `MeetingKeyState`
- Joiner Ed25519 identity key shape-check before roster republication → `crates/mc-service/src/media_admission/identity_key.rs:parse_join_field()`
- `sender_id` allocation, never reused (AES-GCM nonce-reuse invariant R-35) → `crates/mc-service/src/media_admission/sender_id.rs:SenderIdAllocator::allocate()` | fail-closed `0` reply vocabulary → `crates/mc-service/src/media_admission/binding_response.rs:SenderBindingOutcome`
- Forwarding-graph computation (who may receive whose media) → `crates/mc-service/src/media_routing/assignment.rs`, `generation.rs`, `confirm.rs` | client send directive + codec parse → `crates/mc-service/src/media_signaling/directive.rs:AudioEncoding::parse_codec()`
- MH ordinal scoping — an id cannot resolve outside its enclosing entity → `crates/mh-service/src/routing/mod.rs:SenderId::from_wire()`, `SlotId::from_wire()`, `StreamNumber::from_wire()` | RegisterMeeting policy apply outcomes → `crates/mh-service/src/session/mod.rs:ApplyOutcome`, `ApplyFailure`
- Keyless, type-blind forward (reads payload length only) → `crates/mh-service/src/media/forward.rs:forward_one()` | datagram size cap at the transport edge → `crates/mh-service/src/media/ingress.rs`
- Per-connection transport seam (declaration) → `crates/mh-service/src/transport/mod.rs`; production impl → `crates/mh-service/src/webtransport/media_transport.rs`
- Media-path telemetry deny (directory-scoped, fails loudly on scope drift rather than disarming) → `scripts/guards/simple/media-telemetry-deny.yaml`, `crates/dt-guard/src/media_telemetry_deny.rs` | the one permitted dev-only trace sibling → `crates/mh-service/src/observability/per_frame_trace.rs:record()`
- Cross-language frame vectors as the Rust/TS crypto drift anchor → `proto/test-vectors/frame-v2.vectors.json`, guard `scripts/guards/simple/validate-frame-vectors.sh`, non-production generator `crates/media-vector-gen/src/crypto.rs`

## Code Locations — Client SFrame Stack & Credential-Leak Controls
- HKDF extract/expand, SFrame info/nonce derivation → `packages/sdk-core/src/media/frame/sframeKeySchedule.ts:deriveSframeKeys()`, `sframeNonce()` | AES-256-GCM seal/open, KEK unwrap, wrap nonce → `packages/sdk-core/src/media/frame/sframe.ts:sealSframe()`, `openSframe()`, `unwrapTransmitKey()`, `wrapNonce()`
- Key-id pack/unpack with per-field range checks → `packages/sdk-core/src/media/frame/keyId.ts:packKeyId()`, `unpackKeyId()` | Frame decode + extension parsing (untrusted input) → `packages/sdk-core/src/media/frame/frameCodec.ts:decodeFrame()`, `buildPublisherRegion()` | wire constants generated from the vectors, do not edit → `packages/sdk-core/src/media/frame/wireConstants.ts`
- Bounded reject taxonomy (reasons carry no crypto oracle) → `packages/sdk-core/src/media/frame/rejectReason.ts` | verify/decrypt receive path → `packages/sdk-core/src/media/frame/receivePath.ts`
- Ed25519 meeting identity holder → `packages/sdk-core/src/media/setup/identity.ts:MeetingIdentity` | KEK intake sink at the signaling boundary → `packages/sdk-core/src/signaling/kekIntake.ts:takeMeetingKek()` | KEK source seam → `packages/sdk-core/src/media/setup/kekSource.ts`
- Roster verification keys, fail-closed on an absent key → `packages/sdk-core/src/media/setup/rosterKeys.ts:RosterIdentityKeys` | transmit-key generation/rotation, sequence never resets → `packages/sdk-core/src/media/lifecycle/transmitKeys.ts:TransmitKeyManager`
- Token-only join traffic assertion + `redact` helper → `packages/web-app/e2e/fixtures.ts:assertTokenOnlyJoinTraffic` | token/meeting-code format validation → `packages/sdk-core/src/validation/limits.ts:validateUserToken`
- Test-bus secret/PII whitelist projection (media events are scalars only — no key ids, salts, frame bytes, or per-frame sizes, which are ADR-0036 §11's voice-activity trace) → `packages/web-app/src/lib/e2eBus.ts`; prod-absence proof → `packages/web-app/tests/bundle-content.test.ts`
- Trace artifacts contain live tokens — gitignored, do-not-promote → `packages/web-app/e2e/README.md`, `docs/runbooks/devloop-validation.md` §6.7/§8

## Code Locations — AC & Common (Token Issuance, JWT Infrastructure)
- JWT signing/verification, key encryption, bcrypt → `crates/ac-service/src/crypto/mod.rs` | token issuance → `crates/ac-service/src/services/token_service.rs:issue_service_token()`, `issue_user_token()`
- Meeting-token display-name (fail-closed: no users row → generic `NotFound`) → `crates/ac-service/src/handlers/internal_tokens.rs:resolve_meeting_display_name()` | security config + rate limits → `crates/ac-service/src/config.rs`; K8s → `infra/services/ac-service/`
- JWT claims (PII-redacted `Debug`), JWKS client, EdDSA validator → `crates/common/src/jwt.rs` | token manager → `crates/common/src/token_manager.rs:new_secure()` | internal/meeting token types → `crates/common/src/meeting_token.rs` | secret wrapper → `crates/common/src/secret.rs`

## Code Locations — GC (Auth, Access Control, CORS, Telemetry)
- JWT validation → `crates/gc-service/src/auth/jwt.rs` | auth middleware → `crates/gc-service/src/middleware/auth.rs`
- CORS allowlist (fail-closed, wildcard panics router build) → `crates/gc-service/src/config.rs`; layer → `crates/gc-service/src/routes/mod.rs:build_cors_layer`; preflight deny-rewrite → `crates/gc-service/src/middleware/cors_observer.rs`; incident → `docs/runbooks/gc-incident-response.md` Scenario 12
- Telemetry proxy (user-JWT gate, PII drop, size/rate caps) → `crates/gc-service/src/handlers/telemetry.rs`, `crates/gc-service/src/services/telemetry_filter.rs:filter_metrics`
- CSPRNG + role enforcement → `crates/gc-service/src/handlers/meetings.rs` | atomic org-limit CTE, refusal-cause disambiguation, SQLSTATE classifier → `crates/gc-service/src/repositories/meetings.rs:create_meeting_with_limit_check()`, `classify_insert_error()` | unit-only error variants → `crates/gc-service/src/errors.rs` | `join_token_secret` `Debug` redaction → `crates/gc-service/src/models/mod.rs` | participant tracking → `crates/gc-service/src/repositories/participants.rs`

## Code Locations — MC (JWT, WebTransport, Actors, MH Client)
- MC JWT validation + token_type anti-confusion → `crates/mc-service/src/auth/mod.rs:McJwtValidator` | gRPC auth: `McAuthInterceptor`, JWKS `McAuthLayer` (scope `service.write.mc`) → `crates/mc-service/src/grpc/auth_interceptor.rs` | MC→MH OAuth Bearer → `crates/mc-service/src/grpc/mh_client.rs`
- Async RegisterMeeting trigger (first-participant, retry+backoff) → `crates/mc-service/src/webtransport/connection.rs:register_meeting_with_handlers()` | MediaCoordinationService (MH→MC input validation, idempotent re-disconnect) → `crates/mc-service/src/grpc/media_coordination.rs`
- MH connection registry (bound 1000/meeting) + UTF-8 safe truncation → `crates/mc-service/src/mh_connection_registry.rs` | Join trust boundary — display-name cap, `participant_name` length check, fail-closed on missing MH data → `crates/mc-service/src/webtransport/connection.rs`; empty-claim fallback → `crates/mc-service/src/actors/meeting.rs:handle_join()`
- Disconnect: transport-authenticated close (not client-forgeable), raw close-reason never logged → `crates/mc-service/src/webtransport/connection.rs:run_bridge_loop`
- MH assignment store (Redis, no credentials stored) → `crates/mc-service/src/redis/client.rs:MhAssignmentStore` | session binding → `crates/mc-service/src/actors/session.rs`; auth/WT integration tests → `crates/mc-service/tests/`

## Code Locations — MH (Auth, OAuth, TLS)
- gRPC auth layer (JWKS, scope `service.write.mh`) → `crates/mh-service/src/grpc/auth_interceptor.rs:MhAuthLayer` | meeting-token anti-confusion validator → `crates/mh-service/src/auth/mod.rs:MhJwtValidator::validate_meeting_token` | WT accept-path JWT gate → `crates/mh-service/src/webtransport/connection.rs:handle_connection()` | OAuth config (`SecretString`) + QUIC transport parameters → `crates/mh-service/src/config.rs` | TLS+Bearer → `crates/mh-service/src/grpc/gc_client.rs` | error sanitization → `crates/mh-service/src/errors.rs` | JWKS → `infra/services/mh-service/configmap.yaml` | integration tests → `crates/mh-service/tests/`

## Observability (Security-Relevant)
- MC/MH metrics — bounded enum→`&'static str` labels, never PII / client strings / `mh_url` / raw close-reason → `crates/mc-service/src/observability/metrics.rs`, `crates/mh-service/src/observability/metrics.rs` | ADR-0029 | label vocabulary → `crates/common/src/observability/labels.rs`
- AC metric integration suites → `crates/ac-service/tests/`; failure-path adjacency API → `crates/common/src/observability/testing.rs` | ADR-0032 GC audit patterns → `crates/gc-service/src/handlers/meetings.rs`, `docs/observability/metrics/gc-service.md`

## TLS & Certificates
- Dev cert generation + fingerprint SSoT → `scripts/generate-dev-certs.sh`; consumers → `packages/web-app/vite/fingerprints.ts`, `scripts/layer7.sh`; Playwright hash-pin → `packages/web-app/playwright.config.ts`; browser-trust prohibition has two failure modes — prohibited literal added (`crates/dt-guard/src/no_insecure_browser_flags.rs`) vs counter-message removed (`scripts/dev-web.test.sh`)
- MC/MH TLS volume mounts (defaultMode 0400) → `infra/services/mc-service/`, `infra/services/mh-service/`; WebTransport UDP ingress → `infra/services/mh-service/network-policy.yaml`, `infra/kind/kind-config.yaml`; test-time self-signed rigs → `crates/mh-service/tests/common/accept_loop_rig.rs`

## Dependency Audit & Supply Chain
- Layer-6 audit dep-change gate → `scripts/lang/_audit_gate.sh`; glob predicate → `scripts/lang/_changed_helpers.sh`; ambient safety net → `.github/workflows/audit-scheduled.yml` | ADR-0033 §§3,11
- Audit-suppression governance (fail-secure to zero, drift-checked) → `audit-suppressions.toml`, `.pnpm-audit-ignore.json`, guard `scripts/audit-suppressions-check.sh` | transitive overrides → root `package.json` | toolchain pin as a security property → `.npmrc`, `.nvmrc`, `infra/devloop/Dockerfile` | release build profile → `crates/dt-guard/src/release_build_profile.rs`

## Devloop Container, Story Runner, Cluster Isolation
- Container isolation → ADR-0025 | cluster helper trust model → ADR-0030 | helper binary arg/gateway validation → `crates/devloop-helper/src/commands.rs`; auth token (CSPRNG, constant-time compare, 0600) → `crates/devloop-helper/src/auth.rs`
- Runner container-boundary gate + substrate probe → `scripts/workflow/preflight-story.sh` | sentinel-gated test seams and destructive-path containment → `scripts/workflow/run-story.sh:__any_seam_override_present()`; hermetic suite → `scripts/workflow/run-story.test.sh`
- Command-injection surface — task prompt passed by file path, `specialist`/`continue_slug` character floors → `scripts/workflow/run-story.sh` | failure classification + audit-suppression gate → `canary_classify()` | headless completion enforcement → `scripts/workflow/devloop-stop-hook.sh`
- Story manifest as a parsed trust boundary — validated `Slug` newtype, fence-safe emit → `crates/dt-story/src/manifest.rs:to_block_body()` | read-path bail on unterminated fence → `crates/dt-story/src/markdown.rs:find_manifest_block()` | class-drift guard → `scripts/guards/simple/validate-slug-class-sync.sh` | Gate-2 verdict threat model (anti-drift, NOT anti-forgery) → `scripts/lang/_gate2_binding.sh`; self-test → `scripts/guards/simple/selftest-gate2-verdict.sh`
- Env-test URL validation (scheme, credential rejection) → `crates/env-tests/src/cluster.rs:parse_host_port()`; per-run org subdomain (required, DNS-label validated) → `crates/env-tests/src/fixtures/auth_client.rs:resolve_org_subdomain()`, browser side → `packages/web-app/e2e/env.ts` | per-instance env-config guard → `crates/dt-guard/src/env_config.rs`
- Per-run tenant isolation for Layer 7 → `infra/kind/scripts/setup.sh:provision_run_org()`; CSPRNG subdomain + AC org-resolution probe → `scripts/layer7.sh`; credential-containment assertions → `scripts/setup.test.sh`; drift guard → `scripts/guards/simple/validate-subdomain-regex-sync.sh` | Imperative secret creation + input validation → `infra/kind/scripts/setup.sh`, `infra/kind/scripts/teardown.sh` | network policies (per-service ingress/egress; MC→MH:50053, MH→MC:50052) → `infra/services/mc-service/network-policy.yaml`, `infra/services/gc-service/network-policy.yaml` | health probes → `crates/mc-service/src/observability/health.rs`
