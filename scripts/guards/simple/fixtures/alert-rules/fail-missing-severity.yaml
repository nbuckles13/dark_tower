# Both-modes fixture: missing severity label. Must fail.
groups:
  - name: fx-page
    rules:
      - alert: FxNoSeverity
        expr: up == 0
        for: 1m
        labels:
          service: fx-service
        annotations:
          summary: "Fx down"
          description: "Fx is down."
          impact: "User-visible."
          runbook_url: "docs/runbooks/gc-incident-response.md#scenario-4-complete-service-outage"
