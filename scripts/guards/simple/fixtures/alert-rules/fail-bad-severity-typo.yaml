# Fixture: severity typo 'criitcal'. Must fail (typo not in allowed severity set).
groups:
  - name: fx-legacy
    rules:
      - alert: FxSeverityTypo
        expr: up == 0
        for: 1m
        labels:
          severity: criitcal
          service: fx-service
        annotations:
          summary: "Fx down"
          description: "Fx is down."
          impact: "User-visible."
          runbook_url: "https://example.com/runbooks/fx.md"
